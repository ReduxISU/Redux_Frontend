# Base URL of a running Redux backend instance (see https://github.com/ReduxISU/Redux).
# Consumed server-side by pages/api/redux/[...path].js — never exposed to the browser directly.
REDUX_BASE_URL=http://127.0.0.1:27000/
